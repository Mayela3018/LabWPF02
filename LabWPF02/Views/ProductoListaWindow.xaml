<Window x:Class="LabWPF02.Views.ProductoListaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lista de Productos" Height="450" Width="550"
        WindowStartupLocation="CenterScreen">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="60"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0" Background="{StaticResource ColorPrimario}">
			<Grid Margin="20,0">
				<TextBlock Text="📋 Lista de Productos" Foreground="White" FontSize="18" FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Left"/>
				<Button Width="40" Height="32" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="Transparent" BorderThickness="1" BorderBrush="White"
                        Click="BtnVolver_Click">
					<TextBlock Text="←" Foreground="White" FontSize="16" FontWeight="Bold"/>
				</Button>
			</Grid>
		</Border>

		<Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="20">
			<DataGrid x:Name="dgProductos" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
				<DataGrid.Columns>
					<DataGridTextColumn Header="📦 Nombre" Binding="{Binding Nombre}" Width="*"/>
					<DataGridTextColumn Header="🏷️ Categoría" Binding="{Binding Categoria}" Width="*"/>
					<DataGridTextColumn Header="📏 Unidad" Binding="{Binding UnidadMedida}" Width="*"/>
				</DataGrid.Columns>
			</DataGrid>
		</Border>
	</Grid>
</Window>